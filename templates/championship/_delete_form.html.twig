<form method="post" action="{{ path('app_championship_delete', {'id': championship.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ championship.id) }}">
    <button class="btn">Delete</button>
</form>
